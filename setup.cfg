[metadata]
name = smart-renamer
version = 0.1.0
description = Smart regex-based file renamer
author = You
license = MIT

[options]
packages = find:
install_requires =
    # no deps for now

[options.entry_points]
console_scripts =
    smart-renamer = smart_renamer.renamer:main
